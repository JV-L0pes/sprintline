# URL base da API. Em dev, deixe vazio para usar o proxy do Vite (/api -> localhost:8000).
VITE_API_URL=

# Habilita os testes e2e contra um stack real (1 = sim).
CADENCIA_E2E=0
E2E_BASE_URL=http://localhost:5173
