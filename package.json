{
  "name": "burndown-chart",
  "version": "1.0.0",
  "description": "Dashboard interativo de Burndown Chart para equipes de desenvolvimento ágil",
  "main": "index.js",
  "scripts": {
    "start": "http-server -p 3000",
    "build": "echo 'Build completed'",
    "deploy": "gh-pages -d .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "chart.js": "^4.4.1",
    "aos": "^2.3.4"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "http-server": "^14.1.1"
  }
}
